import React from 'react'

const LogoWc = ({ className }) => {
   return (
      <h1 className={`font-semibold tracking-wide font-poppins relative ${className}`} >
      <span className="font-bold text-3xl animate-gradient text-transparent bg-clip-text bg-gradient-to-r from-red-600 via-yellow-500 to-blue-500">
         W
      </span>
   arga
      < span className = "font-bold text-3xl animate-gradient text-transparent bg-clip-text bg-gradient-to-r from-red-600 via-yellow-500 to-blue-500" >
         C
         </span >
   ek
      </h1 >
   )
}

export default LogoWc